spring.application.name=interviewace-backend
# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/interviewace_db
spring.datasource.username=root
spring.datasource.password=Pranup@786

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server port (optional)
server.port=8080

# JWT secret (you can replace with secure key later)
app.jwt.secret=MySecretKeyForJWT
app.jwt.expiration=3600000
